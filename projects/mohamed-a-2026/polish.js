(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const progress=document.querySelector('[data-scroll-progress]');let scheduled=false;function paintProgress(){scheduled=false;if(!progress)return;const max=Math.max(1,document.documentElement.scrollHeight-innerHeight);const ratio=Math.min(1,Math.max(0,scrollY/max));progress.style.transform=`scaleX(${ratio.toFixed(4)})`}addEventListener('scroll',()=>{if(!scheduled){scheduled=true;requestAnimationFrame(paintProgress)}},{passive:true});paintProgress();
const zones=[document.querySelector('[data-pyro-zone]'),document.querySelector('[data-finale-zone]')].filter(Boolean);if(reduce||!('IntersectionObserver'in window)){zones.forEach(z=>z.classList.add('is-live'));return}const fired=new WeakSet();const observer=new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting&&!fired.has(entry.target)){fired.add(entry.target);entry.target.classList.add('is-live');setTimeout(()=>entry.target.classList.remove('is-live'),1450)}}},{threshold:.48,rootMargin:'-4% 0px -8%'});zones.forEach(z=>observer.observe(z));
const finale=document.querySelector('[data-finale-zone]');if(finale&&!matchMedia('(pointer: coarse)').matches){finale.addEventListener('pointermove',event=>{const r=finale.getBoundingClientRect();const x=(event.clientX-r.left)/r.width-.5;const y=(event.clientY-r.top)/r.height-.5;finale.style.setProperty('--fx',x.toFixed(3));finale.style.setProperty('--fy',y.toFixed(3))},{passive:true})}
})();
